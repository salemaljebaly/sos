export enum ReportType {
    FIRE = "Fire",
    AMBULANCE = "Ambulance",
    ACCIDANT = "Accidant"
}

export enum ReportState {
    PENDING = "pending",
    PROCESSING = "processing",
    DONE = "done"
}